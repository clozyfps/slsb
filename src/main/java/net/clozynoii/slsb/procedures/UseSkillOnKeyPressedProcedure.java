package net.clozynoii.slsb.procedures;

import net.minecraftforge.eventbus.api.Event;

import javax.annotation.Nullable;

public class UseSkillOnKeyPressedProcedure {
public static void execute(
Entity entity
) {
if(
entity == null
) return ;
if (<=0) {if (>=) {if (().isEmpty()) {if (!().isEmpty()) {}}}else if (<) {if (entity instanceof Player _player && !_player.level().isClientSide())
_player.displayClientMessage(Component.literal(("You need"+" [\u00A7c"+new java.text.DecimalFormat("#").format()+"\u00A7r]"+" Mana to use"+" [ "+++"\u00A7r]")), true);}}
}
}
